services:
  postgres:
    image: postgres:18-alpine
    container_name: similarity-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: similarity
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev123